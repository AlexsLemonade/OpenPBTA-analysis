version: 2
jobs:
  run_tests:
    machine:
      docker_layer_caching: true
    working_directory: ~/OpenPBTA-analysis
    steps:
      - checkout

      - run:
          name: Analysis 1
          command: ./scripts/run_in_docker.sh 'echo "Hello, working directory!"'

workflows:
  version: 2
  test-and-deploy:
    jobs:
      - run_tests