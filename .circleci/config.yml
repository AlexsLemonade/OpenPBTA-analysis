version: 2
jobs:
  run_tests:
    machine: true
    working_directory: ~/OpenPBTA-analysis
    steps:
      - checkout

      - run:
          name: Analysis 1
          command: ./scripts/run_in_docker.sh 'echo "Hello, working directory!"'
